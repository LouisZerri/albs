{% extends 'base.html.twig' %}

{% block title %}Forum - À la bonne station
{% endblock %}

{% block body %}
	<div class="max-w-7xl mx-auto px-4 py-8">
		{# En-tête #}
		<div class="mb-10">
			<h1 class="text-5xl font-bold text-gray-900 mb-3">💬 Forum de la communauté</h1>
			<p class="text-xl text-gray-600">Échangez avec d'autres passionnés du métro parisien</p>
		</div>

		{# Statistiques #}
		<div class="mb-8 grid grid-cols-1 md:grid-cols-3 gap-6">
			<div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-6 border border-blue-200 hover:shadow-xl hover:scale-[1.02] transition-all duration-300">
				<div class="flex items-center justify-between">
					<div>
						<p class="text-blue-600 text-sm font-medium mb-2">Discussions</p>
						<p class="text-4xl font-black text-blue-700">{{ totalDiscussions }}</p>
					</div>
					<div class="text-6xl">💬</div>
				</div>
			</div>

			<div class="bg-gradient-to-br from-green-50 to-emerald-100 rounded-xl p-6 border border-green-200 hover:shadow-xl hover:scale-[1.02] transition-all duration-300">
				<div class="flex items-center justify-between">
					<div>
						<p class="text-green-600 text-sm font-medium mb-2">Lignes de métro</p>
						<p class="text-4xl font-black text-green-700">{{ lines|length }}</p>
					</div>
					<div class="text-6xl">🚇</div>
				</div>
			</div>

			<div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl p-6 border border-purple-200 hover:shadow-xl hover:scale-[1.02] transition-all duration-300">
				<div class="flex items-center justify-between">
					<div>
						<p class="text-purple-600 text-sm font-medium mb-2">Activité récente</p>
						<p class="text-4xl font-black text-purple-700">{{ recentDiscussions|length }}</p>
					</div>
					<div class="text-6xl">🔥</div>
				</div>
			</div>
		</div>

		{# Barre de recherche #}
		<div class="mb-8">
			<form method="get" action="{{ path('app_forum') }}" class="flex gap-4">
				<div class="flex-grow relative">
					<input 
						type="text" 
						name="q" 
						value="{{ searchQuery }}"
						placeholder="Rechercher une discussion, un auteur..." 
						class="w-full px-6 py-4 pl-14 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent text-lg shadow-sm"
					>
					<span class="absolute left-5 top-1/2 -translate-y-1/2 text-2xl">🔍</span>
				</div>
				<button type="submit" class="px-8 py-4 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-bold rounded-xl transition-all duration-200 shadow-lg hover:shadow-xl cursor-pointer">
					Rechercher
				</button>
				{% if searchQuery %}
					<a href="{{ path('app_forum') }}" class="px-6 py-4 bg-gray-200 hover:bg-gray-300 text-gray-700 font-medium rounded-xl transition-colors flex items-center">
						✕ Effacer
					</a>
				{% endif %}
			</form>
		</div>

		{# Message si recherche #}
		{% if searchQuery %}
			<div class="mb-6 p-4 bg-blue-50 border border-blue-200 rounded-xl">
				<p class="text-blue-800">
					🔍 Résultats pour "<strong>{{ searchQuery }}</strong>" — {{ recentDiscussions|length }} discussion(s) trouvée(s)
				</p>
			</div>
		{% endif %}

		{# Liste des lignes en accordéon #}
		<div class="mb-10" x-data="{ open: false }">
			<button @click="open = !open" class="w-full flex items-center justify-between bg-white rounded-2xl shadow-lg p-6 hover:shadow-xl transition-all duration-300">
				<div class="flex items-center gap-4">
					<span class="text-4xl">📂</span>
					<div class="text-left">
						<h2 class="text-2xl font-bold text-gray-900">Discussions par ligne</h2>
						<p class="text-gray-600">Sélectionnez une ligne pour voir les discussions</p>
					</div>
				</div>
				<svg class="w-8 h-8 text-gray-600 transition-transform duration-300" :class="{ 'rotate-180': open }" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
				</svg>
			</button>

			<div x-show="open" x-collapse class="mt-4">
				<div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
					{% for line in lines %}
						<a href="{{ path('app_forum_line', {lineNumber: line.number}) }}" class="bg-white rounded-2xl shadow-lg hover:shadow-2xl transition duration-200 p-8 text-center group">
							<div class="inline-flex items-center justify-center w-24 h-24 rounded-full text-white font-bold text-6xl mb-4 group-hover:scale-110 transition duration-200" style="background-color: {{ line.color }}; color: {{ line.textColor }};">
								{% if 'bis' in line.number %}
									{{ line.number|replace({'bis': ''}) }}<span class="text-2xl">bis</span>
								{% else %}
									{{ line.number }}
								{% endif %}
							</div>
							<h3 class="font-bold text-gray-900 text-lg mb-2">Ligne {{ line.number }}</h3>
							<p class="text-sm text-gray-600">{{ line.name }}</p>
						</a>
					{% endfor %}
				</div>
			</div>
		</div>

		{# Discussions récentes / Résultats de recherche #}
		{% if recentDiscussions is not empty %}
			<div class="bg-white rounded-xl shadow-lg overflow-hidden">
				<div class="px-8 py-6 border-b border-gray-200 bg-gradient-to-r from-orange-50 to-red-50">
					<h2 class="text-3xl font-bold text-gray-900">
						{% if searchQuery %}
							🔍 Résultats de recherche
						{% else %}
							🔥 Discussions récentes
						{% endif %}
					</h2>
				</div>

				<div class="divide-y divide-gray-200">
					{% for discussion in recentDiscussions %}
						<div class="p-6 hover:bg-gray-50 transition-colors">
							<div class="flex items-start justify-between">
								<div class="flex-grow">
									<div class="flex items-center gap-2 mb-2">
										{% if discussion.isPinned %}
											<span class="text-lg">📌</span>
										{% endif %}
										{% if discussion.isLocked %}
											<span class="text-lg">🔒</span>
										{% endif %}
										<a href="{{ path('app_discussion_show', {id: discussion.id}) }}" class="font-semibold text-gray-900 hover:text-blue-600 text-lg">
											{{ discussion.title }}
										</a>
									</div>
									<div class="flex items-center gap-4 text-sm text-gray-600">
										<a href="{{ path('app_forum_line', {lineNumber: discussion.line.number}) }}" class="flex items-center gap-2 hover:text-blue-600">
											<div class="w-8 h-8 rounded-full flex items-center justify-center text-white text-sm font-bold" style="background-color: {{ discussion.line.color }}; color: {{ discussion.line.textColor }};">
												{% if 'bis' in discussion.line.number %}
													{{ discussion.line.number|replace({'bis': ''}) }}<span class="text-xs">bis</span>
												{% else %}
													{{ discussion.line.number }}
												{% endif %}
											</div>
											Ligne {{ discussion.line.number }}
										</a>
										<span>•</span>
										<a href="{{ path('app_profile_public', {username: discussion.author.username}) }}" class="flex items-center gap-2 hover:text-blue-600">
											<div class="w-6 h-6 rounded-full overflow-hidden bg-gradient-to-br from-blue-400 to-purple-600 flex items-center justify-center">
												{% if discussion.author.avatarUrl %}
													<img src="{{ asset('uploads/avatars/' ~ discussion.author.avatarUrl) }}" alt="{{ discussion.author.username }}" class="w-full h-full object-cover">
												{% else %}
													<span class="text-white font-bold text-xs">{{ discussion.author.username|first|upper }}</span>
												{% endif %}
											</div>
											{{ discussion.author.username }}
										</a>
										<span>•</span>
										<span>{{ discussion.createdAt|date('d/m/Y à H:i') }}</span>
									</div>
								</div>
								<a href="{{ path('app_discussion_show', {id: discussion.id}) }}" class="text-sm text-gray-500 text-right ml-4 hover:text-blue-600">
									<div class="font-semibold text-gray-900">💬 {{ discussion.replyCount }}</div>
									<div class="mt-1">👁️ {{ discussion.viewCount }}</div>
								</a>
							</div>
						</div>
					{% endfor %}
				</div>
			</div>
		{% elseif searchQuery %}
			<div class="bg-white rounded-xl shadow-lg p-12 text-center">
				<span class="text-6xl mb-4 block">😕</span>
				<h3 class="text-2xl font-bold text-gray-900 mb-2">Aucun résultat</h3>
				<p class="text-gray-600">Aucune discussion ne correspond à "<strong>{{ searchQuery }}</strong>"</p>
				<a href="{{ path('app_forum') }}" class="inline-block mt-4 text-blue-600 hover:text-blue-800 font-medium">
					← Retour au forum
				</a>
			</div>
		{% endif %}
	</div>
{% endblock %}