<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            line-height: 1.6;
            color: #1f2937;
            background-color: #f3f4f6;
            margin: 0;
            padding: 20px;
        }
        .email-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }
        .header {
            padding: 40px 40px 20px 40px;
            text-align: left;
            border-bottom: 1px solid #e5e7eb;
        }
        .logo {
            font-size: 28px;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 8px;
        }
        .tagline {
            color: #6b7280;
            font-size: 14px;
            margin: 0;
        }
        .content {
            padding: 40px;
        }
        .greeting {
            font-size: 20px;
            font-weight: 600;
            color: #1f2937;
            margin: 0 0 20px 0;
        }
        .content p {
            margin: 0 0 16px 0;
            color: #4b5563;
            font-size: 15px;
        }
        .button-container {
            margin: 32px 0;
        }
        .button {
            display: inline-block;
            background: #ef4444;
            color: white !important;
            padding: 14px 32px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 15px;
            transition: background 0.2s;
        }
        .button:hover {
            background: #dc2626;
        }
        .info-box {
            background: #fef2f2;
            border-left: 3px solid #ef4444;
            padding: 16px;
            margin: 24px 0;
            border-radius: 4px;
        }
        .info-box p {
            margin: 0;
            font-size: 14px;
            color: #991b1b;
        }
        .warning-box {
            background: #fffbeb;
            border-left: 3px solid #f59e0b;
            padding: 16px;
            margin: 24px 0;
            border-radius: 4px;
        }
        .warning-box p {
            margin: 0;
            font-size: 14px;
            color: #92400e;
        }
        .footer {
            background: #f9fafb;
            padding: 24px 40px;
            border-top: 1px solid #e5e7eb;
        }
        .footer-text {
            margin: 0;
            color: #9ca3af;
            font-size: 13px;
            text-align: center;
        }
        .divider {
            height: 1px;
            background: #e5e7eb;
            margin: 24px 0;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <div class="logo">🚇 À la bonne station</div>
            <p class="tagline">Réinitialisez votre mot de passe</p>
        </div>
        
        <div class="content">
            <p class="greeting">Bonjour {{ user.username }} 👋</p>
            
            <p>Vous avez demandé à réinitialiser votre mot de passe.</p>
            
            <p>Cliquez sur le bouton ci-dessous pour créer un nouveau mot de passe :</p>
            
            <div class="button-container">
                <a href="{{ resetUrl }}" class="button">Réinitialiser mon mot de passe</a>
            </div>
            
            <div class="warning-box">
                <p>⏰ <strong>Ce lien expire dans 1 heure</strong> ({{ expiresAt|date('d/m/Y à H:i', 'Europe/Paris') }})</p>
            </div>
            
            <div class="info-box">
                <p>⚠️ <strong>Important :</strong> Si vous n'avez pas demandé cette réinitialisation, ignorez cet email. Votre mot de passe actuel reste inchangé.</p>
            </div>
            
            <div class="divider"></div>
            
            <p style="font-size: 13px; color: #9ca3af;">
                Pour votre sécurité, ne partagez jamais ce lien avec qui que ce soit.
            </p>
        </div>
        
        <div class="footer">
            <p class="footer-text">
                © {{ 'now'|date('Y') }} À la bonne station · Tous droits réservés
            </p>
            <p class="footer-text" style="margin-top: 8px;">
                📧 Ceci est un email automatique, merci de ne pas y répondre.
            </p>
        </div>
    </div>
</body>
</html>