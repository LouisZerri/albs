<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}À la bonne station{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🚇</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="bg-gray-50 flex flex-col min-h-screen">
        {# Navigation #}
        <nav class="bg-white shadow-lg">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center">
                        <a href="{{ path('app_home') }}" class="flex items-center space-x-2">
                            <span class="text-3xl">🚇</span>
                            <span class="text-xl font-bold text-gray-900">À la bonne station</span>
                        </a>
                    </div>
                    
                    <div class="flex items-center space-x-4">
                        {% if app.user %}
                            <a href="{{ path('app_lines') }}" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                                Lignes
                            </a>
                            <a href="{{ path('app_profile') }}" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                                Mon profil
                            </a>
                            <span class="text-gray-600">👤 {{ app.user.username }}</span>
                            <a href="{{ path('app_logout') }}" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition duration-200">
                                Déconnexion
                            </a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="text-gray-700 hover:text-blue-600 px-3 py-2 rounded-md text-sm font-medium">
                                Connexion
                            </a>
                            <a href="{{ path('app_register') }}" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition duration-200">
                                Inscription
                            </a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        {# Messages Flash #}
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
            {% for type, messages in app.flashes %}
                {% for message in messages %}
                    <div class="mb-4 p-4 rounded-lg {% if type == 'success' %}bg-green-100 border border-green-400 text-green-700{% elseif type == 'error' %}bg-red-100 border border-red-400 text-red-700{% else %}bg-blue-100 border border-blue-400 text-blue-700{% endif %}">
                        <p>{{ message }}</p>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        {# Contenu principal #}
        <main class="flex-grow max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full min-h-[calc(100vh-20rem)]">
            {% block body %}{% endblock %}
        </main>

        {# Footer #}
        <footer class="bg-white border-t border-gray-200 mt-auto">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
                <p class="text-center text-gray-500 text-sm">
                    🚇 À la bonne station - {{ 'now'|date('Y') }} - Explorez le métro parisien
                </p>
            </div>
        </footer>
    </body>
</html>