{% extends 'base.html.twig' %}

{% block title %}Lignes de métro - À la bonne station{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto">
    <div class="mb-10">
        <h1 class="text-5xl font-bold text-gray-900 mb-3">🚇 Les 16 lignes du métro parisien</h1>
        <p class="text-xl text-gray-600">Cliquez sur une ligne pour voir ses stations et marquer votre progression</p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        {% for line in lines %}
            <a href="{{ path('app_line_show', {id: line.id}) }}" 
               class="bg-white rounded-2xl shadow-lg hover:shadow-2xl transition duration-200 p-8 text-center group">
                <div class="inline-flex items-center justify-center w-24 h-24 rounded-full text-white font-bold text-6xl mb-4 group-hover:scale-110 transition duration-200"
                     style="background-color: {{ line.color }}; color: {{ line.textColor }};">
                    {% if 'bis' in line.number %}
                        {{ line.number|replace({'bis': ''}) }}<span class="text-2xl">bis</span>
                    {% else %}
                        {{ line.number }}
                    {% endif %}
                </div>
                <h3 class="font-bold text-gray-900 text-lg mb-2">Ligne {{ line.number }}</h3>
                <p class="text-sm text-gray-600">{{ line.stations|length }} stations</p>
            </a>
        {% endfor %}
    </div>
</div>
{% endblock %}