{% extends 'base.html.twig' %}

{% block title %}Renvoyer l'email de vérification - À la bonne station{% endblock %}

{% block body %}
<div class="min-h-[calc(100vh-16rem)] flex items-center justify-center py-12">
    <div class="max-w-md w-full">
        <div class="bg-white rounded-2xl shadow-xl p-8">
            <div class="text-center mb-8">
                <span class="text-6xl">📧</span>
                <h1 class="text-3xl font-bold text-gray-900 mt-4">Email de vérification</h1>
                <p class="text-gray-600 mt-2">Vous n'avez pas reçu l'email ?</p>
            </div>

            {% for message in app.flashes('success') %}
                <div class="mb-6 p-4 bg-green-50 border-l-4 border-green-500 rounded-r-lg">
                    <div class="flex items-start">
                        <span class="text-green-500 text-xl mr-3">✓</span>
                        <p class="text-sm text-green-700">{{ message }}</p>
                    </div>
                </div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="mb-6 p-4 bg-red-50 border-l-4 border-red-500 rounded-r-lg">
                    <div class="flex items-start">
                        <span class="text-red-500 text-xl mr-3">⚠️</span>
                        <p class="text-sm text-red-700">{{ message }}</p>
                    </div>
                </div>
            {% endfor %}

            <form method="post" class="space-y-6">
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                        Adresse email
                    </label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                        placeholder="votre@email.com"
                    >
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded-lg transition duration-200 shadow-md hover:shadow-lg cursor-pointer"
                >
                    Renvoyer l'email
                </button>
            </form>

            <div class="mt-6 text-center">
                <p class="text-sm text-gray-600">
                    <a href="{{ path('app_login') }}" class="text-blue-600 hover:text-blue-800 font-medium">
                        ← Retour à la connexion
                    </a>
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}