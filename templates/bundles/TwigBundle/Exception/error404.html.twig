{% extends 'base.html.twig' %}

{% block title %}Station introuvable - À la bonne station{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center px-4 py-12">
    <div class="max-w-2xl w-full text-center">
        {# Animation métro perdu #}
        <div class="relative mb-8">
            <div class="text-9xl font-black text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600 animate-pulse">
                404
            </div>
        </div>

        {# Message principal #}
        <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">
            Oups ! Station introuvable
        </h1>
        
        <p class="text-xl text-gray-600 mb-8">
            On dirait que ce métro s'est perdu dans les tunnels... 🕳️
        </p>

        {# Panneau style RATP #}
        <div class="bg-white rounded-2xl shadow-xl p-6 mb-8 border-l-8 border-blue-500 mx-auto max-w-md">
            <div class="flex items-center gap-4">
                <div class="w-16 h-16 rounded-full bg-gradient-to-br from-red-500 to-red-600 flex items-center justify-center text-white text-2xl font-bold shrink-0">
                    ⚠️
                </div>
                <div class="text-left">
                    <p class="font-bold text-gray-900">Trafic interrompu</p>
                    <p class="text-sm text-gray-600">La page que vous cherchez n'existe pas ou a été déplacée.</p>
                </div>
            </div>
        </div>

        {# Suggestions amusantes #}
        <div class="bg-gray-50 rounded-2xl p-6 mb-8">
            <p class="text-gray-700 font-medium mb-4">🔍 Peut-être cherchiez-vous :</p>
            <div class="flex flex-wrap justify-center gap-3">
                <a href="{{ path('app_home') }}" class="px-4 py-2 bg-white rounded-full shadow-md hover:shadow-lg transition-all hover:scale-105 text-gray-700">
                    🏠 Accueil
                </a>
                <a href="{{ path('app_lines') }}" class="px-4 py-2 bg-white rounded-full shadow-md hover:shadow-lg transition-all hover:scale-105 text-gray-700">
                    🚇 Les lignes
                </a>
                <a href="{{ path('app_forum') }}" class="px-4 py-2 bg-white rounded-full shadow-md hover:shadow-lg transition-all hover:scale-105 text-gray-700">
                    💬 Forum
                </a>
                {% if app.user %}
                    <a href="{{ path('app_profile') }}" class="px-4 py-2 bg-white rounded-full shadow-md hover:shadow-lg transition-all hover:scale-105 text-gray-700">
                        👤 Mon profil
                    </a>
                {% endif %}
            </div>
        </div>

        {# Message rigolo aléatoire #}
        <div class="text-gray-500 text-sm mb-8" x-data="{ 
            messages: [
                '🐀 Même les rats du métro ne connaissent pas cette station...',
                '🎵 « Il est 5 heures, Paris s\'éveille... mais pas cette page »',
                '🚨 Attention à la marche en descendant de cette erreur',
                '📢 Suite à un incident technique, cette page est supprimée',
                '🎫 Votre ticket n\'est pas valable pour cette destination',
                '🚉 Terminus ! Tout le monde descend... sur une autre page',
                '⏰ Le prochain contenu arrive dans ∞ minutes'
            ],
            message: ''
        }" x-init="message = messages[Math.floor(Math.random() * messages.length)]">
            <p x-text="message" class="italic"></p>
        </div>

        {# Bouton retour #}
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="{{ path('app_home') }}" class="px-8 py-4 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-bold rounded-xl shadow-lg hover:shadow-xl transition-all duration-200 flex items-center gap-2">
                <span>🏠</span>
                <span>Retour à l'accueil</span>
            </a>
            <button onclick="history.back()" class="px-8 py-4 bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold rounded-xl transition-all duration-200 flex items-center gap-2 cursor-pointer">
                <span>←</span>
                <span>Page précédente</span>
            </button>
        </div>

        {# Plan de métro stylisé en fond #}
        <div class="mt-12 opacity-20">
            <div class="flex justify-center items-center gap-2">
                {% for i in 1..5 %}
                    <div class="w-4 h-4 rounded-full bg-blue-500"></div>
                    {% if not loop.last %}
                        <div class="w-12 h-1 bg-blue-500"></div>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="flex justify-center mt-2">
                <div class="h-8 w-1 bg-purple-500"></div>
            </div>
            <div class="flex justify-center items-center gap-2">
                {% for i in 1..3 %}
                    <div class="w-4 h-4 rounded-full bg-purple-500"></div>
                    {% if not loop.last %}
                        <div class="w-12 h-1 bg-purple-500"></div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
    </div>
</div>

<style>
    @keyframes train-move {
        0%, 100% { transform: translateX(-10px); }
        50% { transform: translateX(10px); }
    }
    .animate-train {
        animation: train-move 2s ease-in-out infinite;
    }
</style>
{% endblock %}