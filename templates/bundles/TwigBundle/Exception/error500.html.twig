{% extends 'base.html.twig' %}

{% block title %}Panne technique - À la bonne station{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center px-4 py-12">
    <div class="max-w-2xl w-full text-center">
        {# Animation erreur #}
        <div class="relative mb-8">
            <div class="text-9xl font-black text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-500">
                500
            </div>
        </div>

        {# Message principal #}
        <h1 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">
            Panne sur la ligne !
        </h1>
        
        <p class="text-xl text-gray-600 mb-8">
            Nos techniciens sont dans les tunnels pour réparer ça 🛠️
        </p>

        {# Panneau style RATP #}
        <div class="bg-white rounded-2xl shadow-xl p-6 mb-8 border-l-8 border-red-500 mx-auto max-w-md">
            <div class="flex items-center gap-4">
                <div class="w-16 h-16 rounded-full bg-gradient-to-br from-red-500 to-red-600 flex items-center justify-center text-white text-3xl shrink-0 animate-pulse">
                    🚨
                </div>
                <div class="text-left">
                    <p class="font-bold text-gray-900">Incident technique</p>
                    <p class="text-sm text-gray-600">Une erreur s'est produite. Veuillez réessayer plus tard.</p>
                </div>
            </div>
        </div>

        {# Boutons #}
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="{{ path('app_home') }}" class="px-8 py-4 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-bold rounded-xl shadow-lg hover:shadow-xl transition-all duration-200 flex items-center gap-2">
                <span>🏠</span>
                <span>Retour à l'accueil</span>
            </a>
            <button onclick="location.reload()" class="px-8 py-4 bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold rounded-xl transition-all duration-200 flex items-center gap-2 cursor-pointer">
                <span>🔄</span>
                <span>Réessayer</span>
            </button>
        </div>

        {# Message #}
        <p class="mt-8 text-gray-500 text-sm italic">
            🎤 « Suite à un incident technique, le trafic est momentanément interrompu »
        </p>
    </div>
</div>
{% endblock %}